# 운영 당일 관리자 체크리스트

## 1. 브라우저 고정
- 관리자 운영 브라우저는 Chrome만 사용한다.
- Safari는 운영에서 사용하지 않는다.
- 운영 시작 전 `/admin`을 같은 Chrome 프로필에서 한 번 연다.
- 예비 노트북도 같은 방식으로 `/admin`을 한 번 열어 목록 캐시를 만든다.

## 2. 관리자 주소
- 관리자 주소는 `https://smart-mission-race-57839.web.app/admin` 하나만 사용한다.
- 다른 북마크, 다른 배포 URL, Safari 히스토리 화면은 사용하지 않는다.

## 3. 목록 확인
- 왼쪽 상단 상태 문구가 아래 중 하나인지 확인한다.
  - `프로젝트 N개를 불러왔습니다.`
  - `프로젝트 N개를 불러왔습니다. (대체 경로)`
  - `프로젝트 N개를 불러왔습니다. (임시 캐시)`
- 목록이 안 뜨면 같은 Chrome 탭에서 새로고침 1회만 한다.
- 그래도 안 뜨면 예비 노트북으로 즉시 전환한다.

## 4. 프로젝트 고정
- 당일 사용할 프로젝트 하나만 선택한다.
- 다른 프로젝트는 열지 않는다.
- 선택 후 기본정보, 팀, 미션, 운영 탭이 모두 열리는지 확인한다.

## 5. 출력물 확인
- 팀 탭에서 아래 4개를 확인한다.
  - `간편 진행표`
  - `진행표 복사`
  - `진행표 CSV`
  - `진행표 인쇄`
- 출력물이 정상인 뒤에만 현장팀에 전달한다.

## 6. 팀 단말 리허설
- 최소 2개 팀으로 확인한다.
  - 1팀
  - 5팀 또는 다른 경로가 다른 팀
- 로그인 -> 코드 -> 미션 -> 사진 업로드 -> HQ 승인 -> 종료 안내까지 1회 확인한다.

## 7. HQ 리허설
- `/hq`, `/review`, `/photo_approve` 모두 열리는지 확인한다.
- 사진 1건 업로드 후 승인 1회, 재도전 1회 확인한다.

## 8. 장애 대응 기준
- 프로젝트 목록이 안 보여도 데이터가 사라진 것이 아닐 가능성이 높다.
- 먼저 Chrome 유지 여부, 관리자 주소, 예비 노트북 캐시 여부를 확인한다.
- Safari 화면은 운영 기준 화면으로 쓰지 않는다.

## 9. 최종 기준
- 목록 정상
- 프로젝트 선택 정상
- 팀표 출력 정상
- 팀 단말 2개 정상
- HQ 승인 정상
이 5개가 확인되면 운영 시작 가능으로 판단한다.

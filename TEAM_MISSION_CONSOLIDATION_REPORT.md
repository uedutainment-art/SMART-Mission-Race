# 팀/미션 통합 리포트 (팀배치표 기준)

## 1) 결론 요약
- 권장 전략: `통합 후 제거`
- 이유:
  - 운영 중 프로젝트에서 즉시 제거를 먼저 하면 데이터 공백/권한 혼선/화면 불일치 위험이 큼
  - 현재 구조는 이미 `routing + teamOverrides + legacy fallback`로 브릿지 구간을 만들 수 있어 안전 전환이 가능
  - CSV/승인/자동이동 등 실제 운영 경로를 단계별로 검증하며 제거 가능

## 2) 중복 기능 진단 (팀 vs 미션)
| 중복 영역 | 현재 중복 상태 | 운영 리스크 | 통합 기준(단일 기준) | 처리 권고 |
|---|---|---|---|---|
| 팀 비번 관리 | 팀 화면/미션 맥락에서 분산 참조 | 비번 변경 누락, 승인 실패 | `teams/{teamId}/profile.password` | 통합 후 레거시 참조 제거 |
| 미션 설정 저장 | `routing`, `teamOverrides`, `teams/.../config/missions` 혼재 | 같은 팀/미션 값이 경로마다 달라짐 | 기본=`routing`, 예외=`teamOverrides` | `config/missions` 점진 제거 |
| 미션 키 배치 | 배치표/개별 팀 편집 동시 운영 | START~LAST 불일치 | `routing.missionKeys + routing.routes` | 배치표 단일화 |
| 슬롯 계산 | 화면별 로컬 계산 중복(과거) | 승인 조건 불일치 | 공통 모듈(`photo_slots`, `mission_schema`) | 완료/유지 |
| 상태 초기화 | 화면별 기본 상태 생성 중복(과거) | 스텝 상태 불일치 | 공통 모듈(`mission_state`) | 완료/유지 |
| CSV 포맷 | 표준/레거시 혼재 | 가져오기 실패, 운영자 혼란 | 표준 CSV 고정 + 레거시 import만 허용 | 레거시 export 제거 후보 |

## 3) 팀배치표 표준 (한눈에 보기)
- 기준: 세로=`팀`, 가로=`팀 비번 -> START -> A/B/C... -> LAST -> 검증`
- 표준 컬럼 순서:
  1. `team`
  2. `team_password`
  3. `START`
  4. `A`
  5. `B`
  6. `C` (필요 시 확장)
  7. `LAST`
  8. `validation`

예시:

| team | team_password | START | A | B | C | LAST | validation |
|---|---|---|---|---|---|---|---|
| Team1 | 1234 | S-01 | A-11 | B-02 | C-07 | L-03 | OK |
| Team2 | 5678 | S-01 | B-09 | C-01 | A-12 | L-03 | WARN: DUP_PASSWORD |
| Team3 | 8899 | S-01 | C-05 | A-02 | B-08 | L-03 | WARN: MISSING_KEY(B) |

## 4) 통합 정책 (필수)
- 단일 진실 소스(SSOT):
  - 배치/공통 미션 규칙: `projects/{id}/routing`
  - 팀별 예외: `projects/{id}/teamOverrides/{teamId}`
  - 런타임 진행 상태: `projects/{id}/teams/{teamId}/missions`
- 레거시 허용 범위:
  - `teams/{teamId}/config/missions`는 `읽기 폴백`만 허용
  - 신규 저장은 `teamOverrides`만 기록
- 검증 기준:
  - 비번 중복
  - 미션 키 누락/중복/미정의
  - START/LAST 고정 위반
  - 타임/키 타입 불일치

## 5) 자주 안 쓰는 기능 정리 (제거 후보)
- 후보 A: 팀별 전체 미션 직접 저장(`config/missions` 전체 덮어쓰기)
  - 문제: routing 기반과 충돌 가능성 큼
  - 권고: 제거, 필요 시 `override 생성`으로 대체
- 후보 B: 레거시 CSV export (`1타임` 형식 재생성)
  - 문제: 운영 표준 분산
  - 권고: export 제거, import 호환만 유지
- 후보 C: 화면별 로컬 슬롯/상태 계산 함수
  - 문제: 승인 조건 분기 불일치
  - 권고: 공통 모듈로만 유지(이미 진행됨)

## 6) 의사결정: 통합하고 없애기 vs 없애고 통합하기
- 권장: `통합하고 없애기`
  - 적용 시점: 운영 중/실데이터 유지/롤백 필요할 때
  - 장점: 장애 위험 최소, 단계별 검증 가능
  - 단점: 전환 기간 동안 코드가 잠시 복잡
- 비권장(예외적 허용): `없애고 통합하기`
  - 적용 시점: 신규 프로젝트/데이터가 비어있고 운영 리허설 환경일 때만
  - 장점: 코드 단순화 빠름
  - 단점: 데이터 손실/경로 누락 시 즉시 장애

## 7) 실행 순서 (권장)
1. `routing + teamOverrides` 저장/조회 경로를 전 화면/함수에 우선 적용
2. 팀배치표 리포트(전체 CSV, 문제팀 CSV)로 경고 0건까지 정리
3. 운영 시나리오 E2E 검증 (로그인 -> 미션 -> 업로드 -> 승인/재도전 -> 자동이동)
4. 레거시 `config/missions` 쓰기 차단(읽기 폴백만 유지)
5. 2회차 운영에서 이상 없으면 레거시 의존 코드 제거

## 8) 완료 판정 기준
- 팀배치표 리포트 `validation` 경고 0건
- 동일 팀/미션 조회 시 UI/백엔드 계산 결과 일치
- 승인/재도전/지연 이동 로그와 화면 상태 100% 일치
- 레거시 경로는 읽기 폴백 외 쓰기 발생 0건

# 연수원형 / 야외형 구현 계획서

## 1. 목적

현재 시스템은 `팀`, `미션`, `routing`, `teamOverrides` 중심으로 많이 정리되어 있다.  
다음 단계 목표는 운영자가 실제 행사 준비 방식대로 입력할 수 있게 구조를 단순화하는 것이다.

이번에 확정한 기준은 아래와 같다.

1. 연수원형
- 코드: 공통 코드 `1, 2, 3 ...`
- 미션: `START, A, B, C ... LAST`
- 팀마다 다른 것은 미션 장소 순서표

2. 야외형
- 코드도 예외가 생길 수 있음
- 미션도 예외가 생길 수 있음
- 운영자는 `기본값 + 예외 추가` 방식으로 입력하고 싶어 함
- 최종 팀표에는 `1-1`, `D-1` 같은 키만 넣어서 적용되면 됨

즉 복잡한 "단계 자산 패키지"보다, 운영자 머릿속 구조와 같은 `코드 기본/예외`, `미션 기본/예외`, `최종 팀표` 구조가 필요하다.


## 2. 최종 결론

구조는 아래 3개 축으로 단순화한다.

1. 코드 라이브러리
- 기본 코드: `1, 2, 3 ...`
- 예외 코드: `1-1, 1-2, 2-1 ...`

2. 미션 라이브러리
- 기본 미션: `START, A, B, C ... LAST`
- 예외 미션: `A-1, A-2, D-1 ...`

3. 최종 팀 배치표
- 각 팀의 각 단계에
  - 코드 키
  - 미션 키
  를 넣음

핵심 원칙:
- 기본값 먼저 입력
- 예외가 필요할 때만 그 코드/미션 아래에 `추가`
- 팀표에서는 최종 키만 넣음


## 3. 설계 원칙

1. 입력은 운영자 기준으로 이해돼야 한다.
- 시스템 내부 구조보다, 운영자가 실제 생각하는 방식이 우선이다.
- "코드 1 밑에 예외 1-1", "미션 D 밑에 예외 D-1"이 바로 보여야 한다.

2. 기본값과 예외를 같은 축에서 관리한다.
- 기본과 예외가 완전히 다른 화면에 있으면 헷갈린다.
- 같은 코드/미션 바로 아래에 예외를 붙이는 방식이 맞다.

3. 팀표에는 실제 최종 키만 넣는다.
- 라이브러리에서 만든 코드/미션 키를 팀표에서 바로 사용한다.
- 없는 키를 넣으면 검증 오류로 잡는다.

4. 연수원형과 야외형을 별도 시스템으로 만들지 않는다.
- 같은 엔진 안에서 `mode`로 분기하되,
- 입력 구조는 최대한 공통으로 유지한다.

5. `START`, `LAST`는 공통값으로 둔다.
- 공통이면 팀표에서 생략 가능하게 한다.
- 필요 시 예외를 만들 수는 있지만 기본은 공통으로 처리한다.


## 4. 용어 정의

### 4.1 코드 키

운영자가 코드 라이브러리에서 만드는 식별자.

예:
- `1`
- `2`
- `2-1`
- `3-1`

의미:
- `2`는 코드 2의 기본값
- `2-1`은 코드 2의 첫 번째 예외값


### 4.2 미션 키

운영자가 미션 라이브러리에서 만드는 식별자.

예:
- `START`
- `A`
- `A-1`
- `A-2`
- `D`
- `D-1`
- `LAST`

의미:
- `A`는 미션 A의 기본값
- `A-1`은 미션 A의 첫 번째 예외값


### 4.3 최종 팀표

세로는 팀, 가로는 단계.

각 칸에는
- 코드 키
- 미션 키
를 넣는다.

예:
- 코드 `2-1`
- 미션 `D-1`


## 5. 연수원형 모델

연수원형은 아래처럼 해석한다.

1. 코드
- 공통 코드 `1, 2, 3 ...`
- 전 팀 동일

2. 미션
- `START, A, B, C ... LAST`
- 팀마다 순서만 다름

즉 연수원형은:
- 코드표는 공통
- 미션표만 팀별 순서가 달라짐

### 5.1 연수원형 입력 예시

코드 라이브러리:
- `1`
- `2`
- `3`
- `4`

미션 라이브러리:
- `START`
- `A`
- `B`
- `C`
- `D`
- `LAST`

팀표:

| 팀 | 1 | 2 | 3 | 4 |
|---|---|---|---|---|
| 1팀 | A | B | C | D |
| 2팀 | D | A | B | C |
| 3팀 | C | D | A | B |
| 4팀 | B | C | D | A |

연수원형 해석:
- 코드 단계는 `1, 2, 3, 4` 공통
- 미션은 팀표에서 지정한 `A/B/C/D` 순서를 따라감


## 6. 야외형 모델

야외형은 코드와 미션 둘 다 예외가 생길 수 있다.  
하지만 운영자는 복잡한 자산 패키지보다 아래 구조를 원한다.

1. 코드 기본값 먼저 생성
2. 예외가 필요하면 코드 아래에 추가
3. 미션 기본값 먼저 생성
4. 예외가 필요하면 미션 아래에 추가
5. 팀표에서 최종 키를 넣어 적용

### 6.1 야외형 코드 라이브러리 예시

- `1`
- `2`
- `2-1`
- `3`
- `4`
- `4-1`

예:
- `2` = 점심 장소 기본 안내 코드
- `2-1` = 점심 장소 예외 안내 코드

### 6.2 야외형 미션 라이브러리 예시

- `START`
- `A`
- `A-1`
- `A-2`
- `C`
- `D`
- `D-1`
- `LAST`

예:
- `A` = 용궁사 기본 미션
- `A-1` = 팔각정
- `A-2` = 기와불사
- `D` = 카페
- `D-1` = 카페 예외 버전

### 6.3 야외형 팀표 예시

사용자 확정안:

| 팀 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 1팀 | A | a-1 | a-2 | C | D |
| 2팀 | A | a-1 | a-2 | D | C |
| 3팀 | A | a-2 | a-1 | C | D |
| 4팀 | A | a-2 | a-1 | D | C |
| 5팀 | C | D | A | a-1 | a-2 |
| 6팀 | C | D | A | a-2 | a-1 |
| 7팀 | D | C | A | a-1 | a-2 |
| 8팀 | D | C | A | a-2 | a-1 |

실제 구현에서는 표기 일관성을 위해 아래 중 하나로 통일한다.

1. 대문자/소문자 그대로 사용
- `A`, `a-1`, `a-2`, `C`, `D`

2. 전부 대문자로 정규화
- `A`, `A-1`, `A-2`, `C`, `D`

추천:
- 화면 표시명은 자유롭게
- 키는 대문자로 정규화


## 7. 실제 운영 입력 방식

### 7.1 연수원형

1. 운영 방식 = `academy`
2. 코드 라이브러리에 `1, 2, 3 ...` 입력
3. 미션 라이브러리에 `START, A, B, C ... LAST` 입력
4. 팀표에 미션 순서 입력
5. 저장 + 적용

### 7.2 야외형

1. 운영 방식 = `outdoor`
2. 코드 라이브러리에 `1, 2, 3 ...` 기본값 입력
3. 예외가 필요한 코드 아래에 `추가`
   - 자동 생성: `1-1`, `1-2`
4. 미션 라이브러리에 `START, A, B, C ... LAST` 기본값 입력
5. 예외가 필요한 미션 아래에 `추가`
   - 자동 생성: `A-1`, `A-2`, `D-1`
6. 최종 팀표에 코드 키 / 미션 키 입력
7. 저장 + 적용


## 8. Admin 화면 구조

### 8.1 기본정보 탭

유지:
- 프로젝트명
- 운영 방식
- 종료 일시
- 교육일
- 팀 수


### 8.2 코드 탭

표 구조:

| 코드 키 | 표시명 | 코드 이미지 | 코드 정답 | 방식 | 예외 추가 |
|---|---|---|---|---|---|
| 1 | 기본 코드 1 | 업로드 | 입력 | 선택 | + |
| 1-1 | 예외 코드 1-1 | 업로드 | 입력 | 선택 | - |
| 2 | 기본 코드 2 | 업로드 | 입력 | 선택 | + |

규칙:
- 기본 코드는 `1, 2, 3 ...`
- 예외 추가 시 해당 코드 아래에 붙음
- 자동 키 생성:
  - `1-1`
  - `1-2`


### 8.3 미션 탭

표 구조:

| 미션 키 | 표시명 | 미션 이미지 | 방식 | 사진수 | 대기 | 예외 추가 |
|---|---|---|---|---|---|---|
| START | 시작 미션 | 업로드 | 선택 | 0 | 0 | + |
| A | 기본 A | 업로드 | 선택 | 0 | 0 | + |
| A-1 | A 예외 1 | 업로드 | 선택 | 2 | 0 | - |
| A-2 | A 예외 2 | 업로드 | 선택 | 1 | 0 | - |
| D | 기본 D | 업로드 | 선택 | 0 | 60 | + |
| D-1 | D 예외 1 | 업로드 | 선택 | 2 | 0 | - |
| LAST | 마지막 미션 | 업로드 | 선택 | 0 | 0 | + |

규칙:
- 기본 미션은 `START`, `A`, `B`, `C`, `D`, `LAST`
- 예외는 바로 아래 추가
- 자동 키 생성:
  - `A-1`
  - `A-2`
  - `D-1`


### 8.4 최종 팀 배치표

세로:
- 팀

가로:
- `START`
- 중간 단계들
- `LAST`

각 칸 입력:
- 코드 키
- 미션 키

예:

| 팀 | 코드1 | 미션1 | 코드2 | 미션2 | 코드3 | 미션3 |
|---|---|---|---|---|---|---|
| 1팀 | 1 | A | 2 | A-1 | 3 | C |
| 2팀 | 1 | A | 2 | A-1 | 3 | D |
| 3팀 | 1 | A | 2 | A-2 | 3 | C |

확장 옵션:
- 팀표를 더 컴팩트하게 하려면
  - 한 셀에 `코드/미션`
  - 예: `2 / A-1`
  로 넣는 방식도 가능

추천 1차 구현:
- 코드 컬럼과 미션 컬럼을 분리
- 검증이 더 쉽고 실수 찾기 쉽다


## 9. 해석 규칙

시스템은 각 팀의 각 단계에서 아래 순서로 읽는다.

1. 코드 키 확인
2. 코드 라이브러리에서 해당 키 로드
3. 미션 키 확인
4. 미션 라이브러리에서 해당 키 로드

즉 최종 해석은:
- `team + step -> codeKey + missionKey`

예:
- Team1 Step2
  - codeKey = `2`
  - missionKey = `A-1`

그러면
- 코드 `2`
- 미션 `A-1`
이 같이 표시된다.


## 10. 검증 규칙

팀표 저장 전 반드시 검사:

1. 코드 키 존재 여부
- 팀표에 있는 코드 키가 코드 라이브러리에 있어야 함

2. 미션 키 존재 여부
- 팀표에 있는 미션 키가 미션 라이브러리에 있어야 함

3. 공통 START/LAST 누락 여부
- 자동 처리 시 경고만
- 수동 처리 시 누락 검사

4. 예외 키 중복 여부
- 같은 부모 아래 `A-1`이 두 번 생성되면 안 됨

5. 이미지/정답 누락 여부
- 코드에 필요한 이미지/정답
- 미션에 필요한 이미지/방식/사진수


## 11. DB 구조 계획

### 11.1 meta

```json
{
  "mode": "academy | outdoor",
  "educationDate": "2026-03-15",
  "endAt": 1770000000000
}
```

### 11.2 codeLibrary

```json
{
  "1": {
    "label": "기본 코드 1",
    "imageUrl": "...",
    "answer": "ST1",
    "mode": "text"
  },
  "1-1": {
    "label": "코드 1 예외",
    "imageUrl": "...",
    "answer": "ST1A",
    "mode": "photo_hq"
  }
}
```

### 11.3 missionLibrary

```json
{
  "START": {
    "label": "시작",
    "imageUrl": "...",
    "mode": "text"
  },
  "A": {
    "label": "기본 A",
    "imageUrl": "...",
    "mode": "photo_hq"
  },
  "A-1": {
    "label": "A 예외 1",
    "imageUrl": "...",
    "mode": "hq"
  }
}
```

### 11.4 teamRouteMatrix

```json
{
  "Team1": {
    "1": { "codeKey": "1", "missionKey": "A" },
    "2": { "codeKey": "2", "missionKey": "A-1" }
  },
  "Team2": {
    "1": { "codeKey": "1", "missionKey": "A" },
    "2": { "codeKey": "2", "missionKey": "A-1" }
  }
}
```


## 12. 구현 순서

### Phase 1. 스키마 정리
- `codeLibrary`
- `missionLibrary`
- `teamRouteMatrix`
- 기존 `routing/teamOverrides`와 연결 전략 정리

### Phase 2. Admin UI 개편
- 코드 탭: 기본/예외 구조
- 미션 탭: 기본/예외 구조
- 팀 탭: 최종 코드/미션 표

### Phase 3. Resolver 개편
- `team + step -> codeKey + missionKey`
- mode별 해석 추가

### Phase 4. 검증/저장
- 키 존재 여부 검사
- 자동 예외 키 생성
- 저장 시 normalize

### Phase 5. 운영 화면 연결
- 팀 화면
- HQ
- Review
- Mobile approve

### Phase 6. 리허설
- 연수원형 샌드박스 1개
- 야외형 샌드박스 1개


## 13. 마이그레이션 전략

기존 프로젝트는 바로 깨지지 않게 유지한다.

1. 기존 `routing/teamOverrides` 프로젝트는 legacy로 읽기 가능
2. 새 프로젝트부터는 새 구조 사용
3. 필요 시 기존 프로젝트를 새 구조로 변환하는 보조 스크립트 제공


## 14. 최종 판단

이 구조의 장점:

1. 운영자가 이해하기 쉽다
2. 연수원형/야외형 둘 다 처리 가능하다
3. 예외를 "기본값 아래 추가"로 표현할 수 있다
4. 팀표가 짧고 명확하다
5. 검증 포인트가 분명하다

핵심 결론:
- 앞으로는 `단계 자산 패키지`보다
- `코드 기본/예외`, `미션 기본/예외`, `최종 팀표`
구조로 구현하는 것이 맞다.

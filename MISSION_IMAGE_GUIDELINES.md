# MISSION IMAGE GUIDELINES

## 권장 결론
- 코드 이미지와 미션 이미지는 `16:9` 비율로 제작한다.
- 권장 원본 크기:
  - `1920 x 1080`
  - `1600 x 900`
- 화면에서는 잘리지 않게 `contain` 기준으로 표시한다.

## 왜 900 x 435가 애매한가
- `900 x 435`는 약 `2.07:1` 비율이다.
- 아이패드와 휴대폰에서 이 비율은 지나치게 가로로 길다.
- 화면에 크게 보이게 맞추면 위아래 또는 좌우가 답답하게 느껴질 수 있다.

## 가장 안전한 제작 규칙
1. 원본은 `16:9`
2. 핵심 텍스트와 핵심 오브젝트는 가장자리에서 떨어뜨린다
3. 상하좌우 `8~10%`는 안전 여백으로 비운다
4. QR, 텍스트, 주요 오브젝트를 가장자리 근처에 두지 않는다

## 권장 템플릿 예시

### 템플릿 A
- 크기: `1920 x 1080`
- 용도: 일반 코드/미션 이미지

### 템플릿 B
- 크기: `1600 x 900`
- 용도: 빠르게 제작할 때

## 이미 만든 900 x 435 파일을 쓸 때
- 그대로 써도 되지만 아래 보정이 필요하다.
1. 원본 파일 자체에 위아래 또는 좌우 여백을 더 넣는다
2. 글자와 핵심 요소를 중앙 쪽으로 모은다
3. 중요한 내용이 화면 가장자리에 닿지 않게 한다

## 표시 규칙
- 팀 화면에서는 `contain`
- 남는 여백은 배경색으로 처리
- 이미지가 잘리지 않는 것이 우선

## 운영 권장
- 새 프로젝트부터는 `16:9`로 통일
- 기존 `900 x 435` 자산은 급하면 사용 가능
- 다만 장기적으로는 모두 `1920 x 1080` 템플릿으로 교체하는 것이 가장 안정적이다
